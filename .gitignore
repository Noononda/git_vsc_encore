secrets.yaml

# 주석 주고싶을때 #

# 파일명을 담으면 된다 (secrets.yaml)

# *.yaml 이런식으로 yaml인 파일을 모두 넣을 수 있다

# logs(logs 이름을 가지는 파일 폴더와 그 내용들)

# 특정 디렉토리 밑에 있는 (logs/debug.log) 로그 디렉토리 밑에 디버그

# logs/*.log(로그 디렉토리 밑에 확장자가 ‘로그’인)

# logs/**/debug.log (로그 폴더 밑에 있는 폴더와 파일을 포함해서 디버그.로그를 제외시켜라)